{% extends 'base.html.twig' %}

{% block title %}Acces{% endblock %}

{% block body %}
    <h1>Acces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acce.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acces_index') }}">back to list</a>

    <a href="{{ path('acces_edit', {'id': acce.id}) }}">edit</a>

    {{ include('acces/_delete_form.html.twig') }}
{% endblock %}
